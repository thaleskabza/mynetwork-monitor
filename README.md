# mynetwork-monitor
This is a my network-monitor, this was motivated by slow network condition yet not knowing how to diagnose so to find a solution 
```bash
brew uninstall --force docker docker-compose docker-machine docker-credential-helper
brew cleanup
```